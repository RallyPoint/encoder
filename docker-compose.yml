version: '3'

services:
  nginx-rtmp:
    build: .
    command: /usr/app/ci/run-local.sh
    volumes:
      - ./ci:/usr/app/ci/
      - ./data:/opt/data/hls
      - ./record-to-replay/:/root/app
    ports:
      - 1935:1935
      - 8080:80
      - 8443:443
    environment:
      - HLS_API=192.168.48.1:9090
